  // Analog input pin for sensor
const int ledPin = 13;    // Digital output pin for LED

void setup() {
  Serial.begin(9600);      // Initialize serial communication
  pinMode(ledPin, OUTPUT); // Set the LED pin as an output
}

void loop() {
  // Read analog data from the sensor
  int sensorValue = analogRead(0);

  // Map the sensor value to the LED brightness (assuming LED is connected to pin 13)
  int brightness = map(sensorValue, 0, 1023, 0, 255);
  analogWrite(ledPin, brightness); // Adjust LED brightness based on sensor value

  // Print sensor data to Serial
  Serial.println(sensorValue);

  delay(100); // Adjust the delay based on your desired sampling rate
}
